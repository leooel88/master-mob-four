import 'package:flutter/material.dart';

// COLORS
const Color gradientViolet = Color.fromRGBO(138, 35, 135, 1.0);
const Color gradientRed = Color.fromRGBO(233, 64, 87, 1.0);
const Color gradientOrange = Color.fromRGBO(242, 113, 33, 1.0);
const Color black = Colors.black;
const Color white = Colors.white;

// RADIUSES
const Radius smallRadius = Radius.circular(15.0);
const Radius normalRadius = Radius.circular(20.0);

// SHADOWS
const BoxShadow basicShadow =
    BoxShadow(color: black, blurRadius: 5.0, spreadRadius: 1.0);

// ICONS
const colorLens = Icon(Icons.color_lens);
const layerClear = Icon(Icons.layers_clear);

// WIDTH
const double lineWidthBasic = 2.0;
const double minLineWidth = 1.0;
const double maxLineWidth = 10.0;

// TEXTS
const Text colorPickerDialogTitleText = Text('Color Picker');
const Text colorPickerCloseButtonText = Text('Close');
